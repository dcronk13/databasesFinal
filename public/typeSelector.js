function selectTypes(id1,id2){
    //used in update pokes page to set drop-down default select to type pokemon already is
    $("#type_selector_1").val(id1);
    $("#type_selector_2").val(id2);
}
